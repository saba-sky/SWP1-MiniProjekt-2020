package de.unibremen.swp.model;

/**
 * These roles are used to limit the accessibility of certain functions so as
 * to prevent unprivileged users from using them.
 */
public enum Role {
    /**
     * What the name says.
     */
    STUDENT,

    /**
     * What the name says.
     */
    TUTOR,

    /**
     * What the name says.
     */
    LECTURER
}
